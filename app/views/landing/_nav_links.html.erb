<% link_class = local_assigns.fetch(:link_class, "rk-nav-link") %>
<% link_data  = local_assigns.fetch(:link_data, {}) %>
<% nav_links.each do |link| %>
  <%= link_to link[:label], link[:href], class: link_class, data: link_data %>
<% end %>
